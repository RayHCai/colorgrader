# colorgrading

react + ts + django + transformers

# Running Locally

1. `clone repo`
2. `cd backend`
    - `pip install -r requirements.txt`
    - `python manage.py server.py`
3. `cd frontend`
    - `yarn`
    - `yarn start`
